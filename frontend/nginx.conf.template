server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router client-side routing - always fall back to index.html for
    # unknown paths instead of nginx's default 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}

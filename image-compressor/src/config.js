const THUMBNAIL = { name: '300X300', maxDimension: 300, quality: 80 };
const DISPLAY = { name: '600X600', maxDimension: 600, quality: 85 };

const VARIANTS = [THUMBNAIL, DISPLAY];

module.exports = { VARIANTS, THUMBNAIL, DISPLAY };
